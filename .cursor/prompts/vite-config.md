Analyze this project and create a production-ready `vite.config.js`.

Requirements:
- This is a static multi-page HTML website.
- Do NOT convert it to React/Vue.
- Do NOT move files into `src`.
- Keep the current folder structure.
- Detect all HTML pages automatically and register them in `build.rollupOptions.input`.
- Ensure `npm run build` generates a complete `dist` folder with all pages.
- Keep asset paths working.
- Optimize for GitHub Pages deployment.
- Add comments explaining the configuration.
- Do not modify existing code unless required.