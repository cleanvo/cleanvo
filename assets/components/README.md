# Shared components notes

Optional location for HTML partial notes or component documentation.
Static GitHub Pages does not include SSI — keep production markup inlined or build-copied into pages.

Phases 3+ will establish shared header/footer markup to mirror across all pages.
